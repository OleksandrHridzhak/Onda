import type {
    ColumnEntry,
    ColumnEntryMeta,
    ColumnEntrySnapshot,
    ColumnEntryValueType,
} from '@onda/shared';

export type {
    ColumnEntry,
    ColumnEntryMeta,
    ColumnEntrySnapshot,
    ColumnEntryValueType,
};

export type ColumnEntryValueMap = Record<string, ColumnEntry>;
