
- Move indexed main and weeks, calender and settings in indexedb/ foder
- Translate indexeddb files and returns msgs
- init and get functions are the same in settings and calendar
- how to convert my objects for using in indexeddb properly
- we don't use weeks    
- fix these diffs in names to camelCase everywhere


Columns
id prop / common things props / uniqProperties object or props for every type but some of them are null

Settings

1 / standard props for every field

Calendar

id / props standard