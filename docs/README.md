# Onda Column Classes Documentation

Документація для нової системи класів колонок в Onda.

## 📚 Документи

### [COLUMN_CLASSES.md](./COLUMN_CLASSES.md)
Повна документація системи класів колонок:
- Структура класів
- Базовий клас Column
- Спеціалізовані класи для кожного типу
- ColumnFactory, ColumnManager, ColumnUtils
- Приклади використання
- Переваги нового підходу

**Коли читати**: Для глибокого розуміння архітектури системи.

### [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md)
Керівництво по інтеграції класів в існуючий код:
- Як імпортувати класи
- Створення нових колонок
- Робота з існуючими даними
- Додавання опцій
- Валідація
- Міграція старого коду
- Повні приклади інтеграції

**Коли читати**: Коли потрібно інтегрувати класи в компоненти.

### [ARCHITECTURE.md](./ARCHITECTURE.md)
Порівняння архітектури до та після:
- Візуальне порівняння структур
- Діаграми архітектури
- Приклади використання
- Переваги нового підходу
- План міграції

**Коли читати**: Для розуміння "чому" і "як" було зроблено рефакторинг.

### [QUICK_REFERENCE.md](./QUICK_REFERENCE.md)
Швидкий довідник для розробників:
- Імпорти
- Створення колонок
- Методи класів
- Утиліти
- Поширені патерни
- Таблиця типів колонок
- Довідник властивостей
- Корисні поради

**Коли читати**: Для швидкого пошуку синтаксису та прикладів.

## 🚀 Швидкий старт

### 1. Імпорт
```javascript
import { ColumnFactory } from './components/utils/columnClasses';
import { ColumnManager, ColumnUtils } from './components/utils/columnManager';
```

### 2. Створення колонки
```javascript
const column = ColumnFactory.createColumn('checkbox', {
  Name: 'My Checkbox',
  CheckboxColor: 'blue',
});
```

### 3. Використання
```javascript
// Оновити значення
column.updateValue('Monday', true);

// Конвертувати для збереження
const data = column.toJSON();
await saveColumn(data);

// Завантажити з БД
const loaded = ColumnFactory.fromJSON(savedData);
```

## 📖 Типи колонок

- **checkbox** - Чекбокс
- **numberbox** - Числове поле
- **text** - Текстове поле
- **multi-select** - Множинний вибір тегів
- **multicheckbox** - Множинні чекбокси
- **todo** - Список завдань (глобальний)
- **tasktable** - Таблиця завдань (глобальна)

## 🎯 Основні класи

### Column (Базовий клас)
Містить загальну функціональність для всіх колонок.

### ColumnFactory
Створення екземплярів колонок.

### ColumnManager
Управління екземплярами (конвертація, валідація, клонування).

### ColumnUtils
Корисні утиліти (перевірка типів, розрахунки, стилі).

## ✅ Переваги

1. **Інкапсуляція** - Логіка кожного типу в своєму класі
2. **Валідація** - Вбудована перевірка даних
3. **Методи** - addOption(), addTodo(), markTaskDone() і т.д.
4. **Розширюваність** - Легко додавати нові типи
5. **Сумісність** - Працює з існуючим кодом

## 🔄 Сумісність

Система повністю сумісна з існуючим кодом:
- `getColumnTemplates()` використовує класи під капотом
- Всі класи конвертуються в/з JSON
- Існуючі дані підтримуються через `fromJSON()`

## 📝 Приклади

### Створення нової колонки
```javascript
const column = ColumnFactory.createColumn('multi-select', {
  Name: 'Tags',
  Options: ['Work', 'Personal'],
  TagColors: { 'Work': 'blue', 'Personal': 'green' },
});
```

### Додавання опції
```javascript
const column = ColumnFactory.fromJSON(existingData);
column.addOption('Urgent', 'red');
await saveColumn(column.toJSON());
```

### Робота з todo
```javascript
const todo = ColumnFactory.fromJSON(existingTodoColumn);
todo.addTodo('Complete documentation');
todo.updateTodo(todoId, { completed: true });
await saveColumn(todo.toJSON());
```

## 🧪 Тестування

Тести знаходяться в кореневій директорії (не коммітяться):
- `test-column-classes.js` - Тести класів
- `test-file-templates.js` - Тести шаблонів
- `test-column-manager.js` - Тести менеджера

Запуск:
```bash
node test-column-classes.js
node test-file-templates.js
node test-column-manager.js
```

## 📁 Файли

- `/render/src/components/utils/columnClasses.js` - Класи колонок
- `/render/src/components/utils/columnManager.js` - Менеджер та утиліти
- `/render/src/components/utils/fileTemplates.js` - Шаблони (оновлено)

## 🤝 Внесок

При додаванні нового типу колонки:
1. Створіть новий клас, що наслідує `Column`
2. Реалізуйте специфічні методи та властивості
3. Додайте тип в `ColumnFactory.createColumn()`
4. Оновіть документацію
5. Додайте тести

## 📞 Питання?

Дивіться відповідні розділи документації:
- Як використовувати → INTEGRATION_GUIDE.md
- Чому так зроблено → ARCHITECTURE.md
- Швидка довідка → QUICK_REFERENCE.md
- Детальна інформація → COLUMN_CLASSES.md
